# Weekly Workflow

1. Run `bash scripts/run_weekly.sh`.
2. Review `outputs/reports/weekly_report.md`.
3. Inspect proxy stability, structural breaks, and ranking transitions in the dashboard.
4. Compare current baskets with the previous week before updating any discretionary research views.

Weekly runs refresh:
- validation snapshots
- transition history
- basket snapshots
- export summaries
